<%= form_for([@user, @user.links.build]) do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :link %><br>
    <%= f.text_area :link %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
