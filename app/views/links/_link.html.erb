<p>
  <strong>Title:</strong>
  <%= link.title %>
  <strong>Link:</strong>
  <%= link.link %>
  <strong>Category:</strong>
  <%= category_name(link.category_id) %>

  <%= link_to 'Readed ', [link.user, link],
               method: :delete,
               data: { confirm: 'Are you sure already read?' } %>
</p>