<div class="center ">
	<div class="container-fluid well ">
		<div class="row-fluid">
			<div class="col-md-4" >
				<img src="https://secure.gravatar.com/avatar/" >
			</div>

			<div class="col-md-4">
				<h1> <%= @user.name %></h3>
				<h2>Email: <%= @user.email%></h2>
				<h2>Links for read:  <%= @user.links.count%> <%= link_to "See Links", links_path %></h2>
				<h2>Categories in use: <%= @user.categories.count%> <%= link_to "Add new category", category_path %></h2>

				<%= link_to "Alter profile", edit_user_path(current_user),class: "btn btn-lg btn-primary"  %>
			</div>
		</div>
	</div>
